name: CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  embedded:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install embedded dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            cppcheck \
            gcc-arm-none-eabi \
            binutils-arm-none-eabi \
            libnewlib-arm-none-eabi

      - name: Show embedded tool versions
        run: |
          cmake --version
          ninja --version
          cppcheck --version
          arm-none-eabi-gcc --version
          arm-none-eabi-objdump --version

      - name: Configure debug
        run: cmake --preset debug

      - name: Build app debug
        run: cmake --build --preset debug --target cursada_mc2_app

      - name: Run cppcheck
        run: cmake --build --preset debug --target cppcheck

      - name: Configure release
        run: cmake --preset release

      - name: Build app release
        run: cmake --build --preset release --target cursada_mc2_app

      - name: Upload debug artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cursada_mc2_app-debug
          path: |
            build/debug/cursada_mc2_app.elf
            build/debug/cursada_mc2_app.bin
            build/debug/cursada_mc2_app.hex
            build/debug/cursada_mc2_app.map
          if-no-files-found: error

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cursada_mc2_app-release
          path: |
            build/release/cursada_mc2_app.elf
            build/release/cursada_mc2_app.bin
            build/release/cursada_mc2_app.hex
            build/release/cursada_mc2_app.map
          if-no-files-found: error

  host-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install host test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            gcc \
            gcovr

      - name: Show host tool versions
        run: |
          cmake --version
          ninja --version
          gcc --version
          gcov --version

      - name: Configure host debug
        run: cmake --preset host-debug

      - name: Build host tests
        run: cmake --build --preset host-debug

      - name: Run host tests
        run: ctest --preset host-debug

      - name: Configure host coverage
        run: cmake --preset host-coverage

      - name: Run host coverage
        run: cmake --build --preset host-coverage --target coverage

      - name: Upload host coverage artifacts
        uses: actions/upload-artifact@v4
        with:
          name: host-coverage
          path: |
            build/host-coverage/*.gcov
            build/host-coverage/**/*.gcda
            build/host-coverage/**/*.gcno
          if-no-files-found: error
